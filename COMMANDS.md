# Commands

## Client commands

- `/start` — begin a new AI image order.
- `/cancel` — cancel the current order flow.

## Administrator commands

- `/orders` — show the ten most recent orders.

Use the inline buttons in the payment and generated-image messages to confirm or reject a payment, regenerate an image, cancel an order, or deliver the result.
