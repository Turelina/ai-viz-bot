# Project Status

Current version: MVP 0.3.0.

The bot accepts a client request and reference photo, collects a payment screenshot, verifies payment through Claude Vision, generates an English image prompt through the Engineer Agent, generates image options through Gemini when configured, and lets an administrator deliver the selected result.
