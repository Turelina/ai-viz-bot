# Pydantic Settings

`config/settings.py` defines validated application settings and loads values from `.env`. Copy `.env.example`, fill in deployment-specific values, and never commit the resulting `.env` file.
