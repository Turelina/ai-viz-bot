# python-telegram-bot

The Telegram integration uses asynchronous handlers, a persistent `ConversationHandler`, inline keyboard callbacks, and scheduled cleanup through the application job queue. The bot exposes client commands for starting or cancelling orders and an administrator command for recent orders.
