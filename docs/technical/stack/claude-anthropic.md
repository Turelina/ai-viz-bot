# Claude and Anthropic

The bot uses Anthropic's asynchronous client for the Listener, Manager, Vision, and Engineer agents. The Manager and Engineer generate client-flow responses and image prompts, while Vision analyzes payment screenshots. API credentials are supplied through `ANTHROPIC_API_KEY`.
