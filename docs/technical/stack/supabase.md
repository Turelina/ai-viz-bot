# Supabase

Supabase stores orders and conversation messages and hosts reference photos in Storage. The database layer recreates its client after transient failures and uses signed URLs for reference files. Initialize the schema with `scripts/setup_database.py`.
