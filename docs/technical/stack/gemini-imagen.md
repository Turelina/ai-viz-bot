# Gemini Image Generation

When `GEMINI_API_KEY` is configured, the bot calls `gemini-3-pro-image-preview` to generate several 2K image options from the Engineer Agent's prompt and an optional reference image. Without the key, administrators receive the prompt and can generate or deliver the image manually.
