# User Journey

The client selects `/start`, describes the desired changes, sends a property photo, receives payment details, and sends a receipt screenshot. After confirmation, the client receives the completed visualization. The administrator only needs to review exceptions and deliver the final image when required.
